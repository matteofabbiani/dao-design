# Dao site

Static. Deploy the whole `site/` folder as the project root on Vercel (framework: Other, no build) or Cloudflare Pages / Netlify (publish directory: `site`).

Routes: `/`, `/story`, `/evening`, `/reserve`. `vercel.json` handles clean URLs on Vercel, `_redirects` on Netlify/Cloudflare.
